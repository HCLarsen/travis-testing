language: ruby

rvm:
 - 2.2.2
 - 2.3
 - 2.4

gemfile:
 - gemfiles/rails-5.0.gemfile
 - Gemfile

before_install:
  - "gem install bundler --pre"
  - "gem update bundler"

before_script: "bundle update"

script: "bin/test"

notifications:
  email: false
